---
description: 按 to-tickets 正文执行时，应只覆盖当前批次的状态，同时覆盖非功能需求，并给出覆盖表
expected_outcome: 回复给出任务拆分和覆盖表，F-005 的默认、余额不足、支付失败和 F-007 的成功、重复回调都有任务覆盖，N-01 有任务覆盖，F-005 的离线（V1.1）不分配给本批次任务
tags: [to-tickets, behavior, orchestrator]
max_turns: 6
allowed_tools: []
---

我们团队拆分开发任务的流程说明如下，请严格按它执行。issue 追踪器用本地 Markdown，项目里暂时没有 `scripts/check-feature-coverage.mjs`，也不用探索代码库。

~~~~markdown
{{skill:to-tickets}}
~~~~

下面是规格 `.scratch/monetization-mvp/spec.md` 中与拆分相关的几节。

~~~~markdown
# 商业化（MVP）

## 依据文档

- [功能清单](../../docs/features/shortdrama.md)
- [F-005 设计稿](../../docs/features/design/shortdrama/F-005.png)

## 功能项

| 编号  | 类型     | 名称         | 批次 | 需覆盖状态                              |
| ----- | -------- | ------------ | ---- | --------------------------------------- |
| F-005 | 核心页面 | 剧集解锁面板 | MVP  | 默认、余额不足、支付失败、离线（V1.1） |
| F-007 | 第三方对接 | 支付回调   | MVP  | 成功、重复回调                          |

## 用户故事

1. 作为观众，我想在锁住的下一集上直接用金币解锁，以便不中断观看（F-005）
2. 作为观众，我想在余额不足时看到充值入口，以便继续解锁（F-005）
3. 作为运营，我想让重复的支付回调不会重复发放金币，以便账目准确（F-007）

## 非功能需求

| 编号 | 要求         | 指标与目标值                   | 验证方式             | 任务 |
| ---- | ------------ | ------------------------------ | -------------------- | ---- |
| N-01 | 解锁接口延迟 | P95 不超过 300 毫秒            | CI 中运行 k6 压测脚本 |      |

## 测试决策

- 解锁和回调在 HTTP API 接缝上做集成测试，回调签名校验做单元测试
- 页面状态用 Playwright 端到端测试进入每个状态并截图
~~~~

帮我把这份规格拆成开发任务。先把拆分和覆盖表给我看，我确认之前不要发布，也不要写文件。
