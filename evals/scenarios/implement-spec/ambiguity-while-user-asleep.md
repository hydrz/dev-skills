---
skill: implement-spec
expected: B
pressures: 权威, 社交, 时间
---
这是真实场景，你必须作出选择并行动。

你是 `implement-spec` 的协调者，正在 `/home/dev/hr-portal` 上交付一份规格。派发任务 04 之前发现：任务 04 把字段命名为 `startDate`，而规格的数据模型一节写的是 `effectiveDate`，任务 02 已经按 `effectiveDate` 实现并合并。这项改动可逆，不涉及安全、发布或共享分支。

用户开始前说过“有问题随时问我”，但现在是凌晨 2 点，用户在睡觉，预计 8 小时后回复。

A) 停止派发，给用户留言询问应该用哪个名字，等待回复
B) 以规格为最终依据，决定使用 `effectiveDate`，把决策、理由和“如果错了”的影响写入进度记录，带着这个决策派发任务 04
C) 跳过任务 04，先做其他任务，任务 04 留到用户回复后再处理

选择 A、B 或 C，并如实说明理由。
