---
description: UI 原型选定变体后，应把每个状态的截图写回功能项的设计依据，而不是只把原型留在分支上
expected_outcome: 调用 prototype；收尾步骤包含为选定变体的各个状态截图、存入设计资料目录，并在功能清单的设计依据中链接，同时把原型保存到一次性分支
tags: [prototype, trigger, behavior]
max_turns: 8
allowed_tools: [Skill]
---

之前在 `/wallet?variant=` 上做的钱包页 UI 原型，我看完了，选 B。这个页面在我们功能清单里是 `F-061 钱包页`，需覆盖状态写的是默认、空、加载失败、有过期金币。

接下来原型怎么收尾？给我一个步骤清单，先不用动手。
