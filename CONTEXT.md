# dev-skills 通用语言

本文件记录项目特有的领域概念及其关系。普通工程词按照各自原义使用；中文语气、排版和中英混排规则见 [docs/chinese-writing.md](./docs/chinese-writing.md)。

## skill 体系

**skill**：一个 `SKILL.md` 及其配套参考文件，由 agent 运行环境按需加载。插件是分发多个 skill 的方式，不是 skill 的同义词。

**仅用户触发**（user-invoked）：只有用户显式输入名称才能触发的 skill。frontmatter 包含 `disable-model-invocation: true`，通常负责编排一段完整流程。

**可自动触发**（model-invoked）：agent 可以按场景主动触发、用户也可以显式触发的 skill，通常提供可复用的方法或约束。

**路由**（router）：帮助用户或 agent 判断下一步该用哪个 skill。本仓库的路由是 `ask-dev-skills`。

**上下文指针**（context pointer）：留在当前上下文中的简短引用，指向需要时再读取的外部材料，并说明读取时机。

**先导词**（leading word）：利用模型已有知识来压缩一组行为的概念词。先导词应优先采用目标读者已经熟悉的表达；自造术语需要承担额外的解释成本。

## 方案澄清

**方案追问**（grilling）：按依赖关系逐轮询问决策问题，直到双方形成共同理解。事实由 agent 查证，决策由用户作出。

**当前待确认项**：前置问题已经确定、当前一轮可以直接询问的全部决策。它是对话中的动态集合，不强制对应某个固定图论术语。

**实施前确认**：开始修改文件、代码或外部状态之前，用户对“要做什么”给出明确确认。流程可以按工作规模简化，这项确认不能省略。

## 领域建模

**通用语言**（ubiquitous language）：代码、文档和对话共同使用的领域词汇。

**术语表**：记录一个项目特有概念、含义及关系的文档。本仓库使用 `CONTEXT.md`。术语表用于消除歧义，不替代通用工程词典。

**ADR**（架构决策记录）：记录难以逆转、缺少上下文时令人意外且确有取舍的重要决策，默认放在 `docs/adr/`。

## 模块设计

**模块**（module）：具有接口和实现的一段设计，尺度可以是函数、类、包或跨层能力。组件、服务和单元仍按各自原义使用，不要求统一替换为“模块”。

**接口**（interface）：调用方正确使用模块所需知道的信息，包括签名、不变量、顺序约束、错误模式、配置和性能特征。API 和函数签名是更具体的工程概念，不是必须替换的禁用词。

**深度**（depth）：接口所提供的杠杆。行为丰富而接口简单的模块较深；接口复杂度接近实现复杂度的模块较浅。

**测试接缝**（seam）：无需修改目标代码即可替换行为的位置。它可以位于模块边界或接口处，但“接缝”和“边界”不总是同义词。

**适配器**（adapter）：在接缝处实现某个接口、连接具体外部能力的代码。

**限界上下文**（bounded context）：一套领域模型和通用语言成立的范围。同一个词在不同上下文中可以有不同含义。

**业务模块**（business module）：实现某个限界上下文的一组代码，拥有自己的数据和对外接口，是模块在业务尺度上的一种形态。它说的是代码组织，限界上下文说的是语言和模型的范围。

**杠杆**（leverage）与**局部性**（locality）：较深的模块让调用方通过较少接口使用更多能力，也让实现变化尽量停留在模块内部。

## 规划与交付

**规格**（spec）：由已经完成的讨论整理而成，可以直接指导实现的需求文档。

**规格拆分表**：大型工作按业务模块和发布批次拆成多份规格时，列出每份规格、它负责的功能项和前后顺序的一张表，写在功能清单中。一份规格对应一个模块的一个批次。

**功能清单**：一项工作要交付的全部功能项。每个**功能项**有固定编号，写明类型（核心页面、普通页面、功能、后台任务、第三方对接）、用到它的角色和端、发布批次，以及需覆盖状态。规格、开发任务和验收都用编号引用功能项。小功能可以在规格中用一节代替独立的清单文件。

**需覆盖状态**：一个功能项必须实现的全部状态，例如默认、加载中、空、出错、锁定、无权限。只做了默认状态，这个功能项不算完成。

**发布批次**：一组一起交付的功能项和状态，例如 `MVP`、`V1.1`。功能清单记录当前批次；规格、任务覆盖和完成验证只核对当前批次。功能项或状态移到更晚的批次属于缩减当前范围，需要用户确认。

**非功能需求**：性能、无障碍、多语言、可观测性等不属于某个功能项、但整份规格都要满足的要求。写在规格中，编号为 `N-01` 的形式，每条都有可测量的目标值和验证方式。

**设计依据**：说明一个功能项应该长什么样、怎么表现的文档。核心页面（首屏、转化路径、核心体验）必须有设计稿、选定的原型变体或视觉规格章节；普通页面可以是一条决定，写明按设计系统实现并参照哪个具体页面。页面以外的功能项，设计依据是对应的行为决策。

**依据文档**：规格和开发任务必须附上链接的长期文档，例如功能清单、设计稿、视觉规格、ADR 和原型分支。代码文件路径很快会过时，不算依据文档。

**开发任务**：从规格或计划中拆出的可执行工作项。发布到 GitHub、GitLab 等追踪器后，可以称为 `issue`；“工单”只用于运维、支持或审批等符合该词习惯的场景。

**端到端最小闭环任务**（tracer bullet）：范围较窄，但完整穿过实现所需各层，完成后即可演示或验证的开发任务。讨论其思想来源时可以括注“曳光弹”。

**前置依赖**：任务开始前必须完成的其他任务。依赖已经满足、尚未认领的任务可以称为“当前可执行任务”或“就绪任务”。

**issue 追踪器**（issue tracker）：承载 issue 和任务依赖关系的工具，例如 GitHub、GitLab，或项目约定的本地 Markdown 文件。

**进度记录**（ledger）：`implement-spec` 写入磁盘的持久化执行状态，用于上下文压缩或会话中断后恢复进度。

**代行决策**（ruling）：执行期间缺少用户输入时，agent 为继续推进而作出的可逆或低风险决定。记录内容包括决定、理由和判断错误的影响，结束时向用户汇总。

## 验证

**反馈回路**（feedback loop）：能够对某个行为给出通过或失败信号的命令。快速、确定且可由 agent 无人值守运行的反馈回路更适合实现和调试。

**变红**（red）：测试或验证针对当前问题给出失败信号。先确认它能准确失败，才能证明它可以发现目标问题。

**先证据后结论**（evidence before claims）：声称完成、修好或通过之前，在当前状态重新运行相应验证并检查完整输出。

**一手资料**（primary source）：事情发生时留下的原始信息，例如会话、源码和官方文档。对这些内容的总结属于二手资料。

## 关系

- 仅用户触发的 skill 可以编排多个可自动触发的 skill；可自动触发的 skill 不应反过来触发仅用户触发的入口。
- 一份规格可以拆成多个开发任务，任务之间通过前置依赖形成执行顺序。
- 大型工作先确定当前批次，把这个批次的功能项分到各份规格，再由开发任务覆盖每个功能项的每个状态和每条非功能需求。清单的“任务”列和规格中非功能需求的“任务”列记录了覆盖情况。
- 增加范围时，agent 记录后可以直接改；缩减范围必须由用户确认。
- 方案追问每轮处理当前待确认项，直到没有被默默假设的决策，并获得实施前确认。
- 较深的模块通过简单接口提供更多能力，在调用侧形成杠杆，在维护侧形成局部性。
- 一个业务模块默认实现一个限界上下文。业务模块之间只通过公开接口或契约交互，内部结构按业务复杂度决定。
