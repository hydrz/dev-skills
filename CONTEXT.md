# dev-skills

一套中文 agent skills，用于真实的软件工程。思想主干取自 mattpocock/skills，吸收 obra/superpowers 的纪律性做法。本文件是整个仓库的**通用语言**：写 skill、写文档、写提交信息时，领域概念一律用这里的词。

## 语言

### Skill 体系

**Skill**：
一个 `SKILL.md` 加上它自带的参考文件，由 harness 加载。
_避免_：技能包、插件（插件是分发单位，不是 skill）

**用户调用**（user-invoked）：
只有人类键入名字才能触发的 skill，frontmatter 带 `disable-model-invocation: true`。职责是**编排**。
_避免_：手动 skill、命令型 skill

**模型调用**（model-invoked）：
模型可以自主触发、人类也可以键入的 skill。职责是承载可复用的**纪律**。
_避免_：自动 skill

**路由**（router）：
一个用户调用 skill，列出其他 skill 以及何时用哪个，本仓库里是 `guide`。
_避免_：索引、菜单

**上下文指针**（context pointer）：
留在 agent 上下文里、指向上下文外材料的引用，并写明何时去读。skill 的 description 就是一种。

**先导词**（leading word）：
预训练里已有的紧凑概念，反复以词的形式出现，用来锚定一整片行为，例如 _曳光弹_、_紧_。

### 对齐

**拷问**（grilling）：
按设计树一轮一轮追问用户，直到双方达成共识。事实由 agent 查，决策由用户定。
_避免_：头脑风暴、访谈

**设计树**（design tree）：
每个决策向下分叉出依赖它的决策所形成的树。

**前沿**（frontier）：
前置条件都已确定、现在就能处理的那一批节点。设计树、工单图、地图都有前沿。

**确认闸门**（approval gate）：
动手实现之前，用户对"要做什么"说出明确的"是"。仪式随规模缩放，闸门不缩放。

### 领域

**通用语言**（ubiquitous language）：
代码、文档、对话共用的一套领域词汇，记录在 `CONTEXT.md`。

**术语表**：
`CONTEXT.md` 本身。只收本项目特有的概念，不含实现细节。
_避免_：词典、wiki

**ADR**（架构决策记录）：
一段话记录一个难以逆转、缺乏上下文会令人意外、且确有取舍的决策，放在 `docs/adr/`。

### 设计

**模块**（module）：
任何有接口和实现的东西，尺度不限：函数、类、包、跨层切片。
_避免_：组件、服务、单元

**接口**（interface）：
调用方正确使用模块所需知道的一切：签名、不变量、顺序约束、错误模式、配置、性能特征。
_避免_：API、签名

**深度**（depth）：
接口处的杠杆，调用方每学一单位接口能用到的行为量。行为多接口小为**深**，接口几乎和实现一样复杂为**浅**。

**接缝**（seam）：
不改动该处代码就能改变行为的位置，模块接口所在的地方。
_避免_：边界

**适配器**（adapter）：
在接缝处满足接口的具体实现，描述角色而非内容。

**杠杆**（leverage）/ **局部性**（locality）：
深度给调用方带来杠杆，给维护者带来局部性。

### 交付

**规格**（spec）：
由对话综合而成、发布到 issue 追踪器的需求文档。

**工单**（ticket）：
`to-tickets` 产出的一条**曳光弹**式垂直切片，声明自己的**阻塞边**。
_避免_：任务（任务在本仓库不作术语）

**曳光弹**（tracer bullet）：
窄但完整地穿过所有层的一条切片，做完就能演示或验证。

**阻塞边**（blocking edge）：
工单 A 必须在工单 B 开始之前完成，则 A 阻塞 B。

**Issue 追踪器**（issue tracker）：
承载 issue 的工具：GitHub、GitLab、本地 `.scratch/` Markdown 约定等。

**台账**（ledger）：
`implement-spec` 写在磁盘上的进度文件，扛过上下文压缩，是恢复现场的依据。

**裁决**（ruling）：
执行中 agent 代替用户做出的决定，按"决定 / 理由 / 错了代价多大"记录，结束时全部上报。

### 验证

**反馈回路**（feedback loop）：
一条能对某个行为给出通过/失败信号的命令。**紧**的回路：快、确定、agent 可无人值守运行。

**变红**（red）：
回路或测试针对这个具体问题给出失败信号。能变红，才证明它抓得住这个问题。

**先证据后结论**（evidence before claims）：
声称"完成 / 修好 / 通过"之前，必须在本轮运行过对应命令并读过输出。

**一手资料**（primary source）/ **二手资料**：
事情发生时的原样（会话本身、源码、官方文档）与对它的摘要（压缩、交接文档、博客）。

## 关系

- 一个**用户调用** skill 可以调用多个**模型调用** skill，但不能调用另一个**用户调用** skill
- 一份**规格**拆成多张**工单**，工单之间由**阻塞边**连成图，图的**前沿**可以并行
- **拷问**遍历**设计树**，逐轮处理其**前沿**
- **深度**产生**杠杆**与**局部性**；**接口**位于**接缝**；**适配器**在接缝处满足接口

## 已澄清的歧义

- "任务"在 superpowers 里指计划中的步骤；本仓库统一用**工单**，不再用"任务"作术语。
- "brainstorming"与"grilling"：本仓库只用**拷问**。规模分档（探针 / 有界 / 架构级）并入 `guide` 与 `grilling`。
