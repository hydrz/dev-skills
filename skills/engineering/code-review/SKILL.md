---
name: code-review
description: 分别检查一组改动是否符合仓库规范和需求规格，并并排报告结果。用于“代码评审、review、评审 PR、检查从某个提交以来的改动”等请求。
---

从两个维度评审 `HEAD` 与用户指定的比较基准之间的 diff：

- **规范**：代码是否符合本仓库成文的编码规范？
- **规格**：代码是否准确实现了来源 issue 或规格？

两个维度分别由**并行子代理**评审，互不影响上下文，然后由本 skill 汇总。

## 过程

### 1. 确定比较基准

用户指定的就是比较基准（提交 SHA、分支名、tag、`main`、`HEAD~5` 等）。用户没有指定时，询问用户。

记下 diff 命令：`git diff <比较基准>...HEAD`（三个点，与 merge-base 比较），以及提交列表命令：`git log <比较基准>..HEAD --oneline`。

继续之前，确认比较基准可以解析（`git rev-parse <比较基准>`）且 diff 不为空。无效的引用或空 diff 应该在这一步报错，而不是在两个并行子代理中报错。

把提交列表、`git diff --stat` 和带上下文的完整 diff（`git diff -U10 <比较基准>...HEAD`）写入临时目录中一个名称唯一的文件，作为**评审材料**。子代理读取这个文件，diff 本身不进入你的上下文。

### 2. 查找规格来源

按以下顺序查找来源规格：

1. 提交信息中的 issue 引用（`#123`、`Closes #45`、GitLab `!67` 等），按 `docs/agents/issue-tracker.md` 中的方式获取。
2. 用户通过参数传入的路径。
3. `docs/`、`specs/` 或 `.scratch/` 下与分支名或功能相符的规格文件。
4. 都没有找到时，询问用户规格在哪里。用户表示没有规格时，跳过**规格**子代理，并报告“无可用规格”。

找到规格后，同时收集它的事实来源：规格或任务链接的范围清单和设计依据。它们与规格一起交给规格子代理。

### 3. 查找规范来源

查找仓库中所有说明代码应该如何编写的文档，例如 `CODING_STANDARDS.md`、`CONTRIBUTING.md`，以及 `CLAUDE.md` 或 `AGENTS.md` 中的约定。

除仓库文档外，**规范**维度始终附带下面的**代码坏味道基线**：一组固定的 Fowler 代码坏味道（《重构》第 3 章）。即使仓库没有任何成文规范，它也适用。使用基线时遵循两条规则：

- **仓库规范优先。** 成文的仓库规范总是优先；仓库明确认可、但基线会标记的写法，不报告。
- **始终是判断性建议。** 每个坏味道都是带标签的启发式判断（例如“可能是依恋情结”），不是硬性违规。工具已经强制检查的内容不报告。

每个坏味道都写成“是什么，怎么修”，逐一对照 diff 检查：

- **神秘命名**：函数、变量或类型的名称无法说明它做什么或存什么。修复：重命名；想不出准确的名称，说明设计本身不清楚。
- **重复代码**：同样的逻辑结构出现在改动的多个 hunk 或文件中。修复：提取共同结构，在两处调用。
- **依恋情结**：一个方法访问另一个对象的数据，比访问自身数据还多。修复：把方法移到它所依赖的数据所在的位置。
- **数据泥团**：同样几个字段或参数总是一起出现，说明缺少一个类型。修复：封装成一个类型并传递它。
- **基本类型偏执**：用基本类型或字符串代替一个应该拥有独立类型的领域概念。修复：为这个概念定义一个小类型。
- **重复的 switch**：针对同一类型的相同 `switch` 或 `if` 级联在改动中反复出现。修复：用多态替代，或让多处共用一张映射表。
- **霰弹式修改**：一个逻辑改动迫使 diff 在多个文件中分散修改。修复：把一起变化的代码集中到一个模块。
- **发散式变化**：一个文件或模块因为几个互不相关的原因被修改。修复：拆分，让每个模块只因一个原因变化。
- **夸夸其谈通用性**：为规格中并不存在的需求添加抽象、参数或钩子。修复：删除或内联，等真实需求出现再加。
- **过长的消息链**：`a.b().c().d()` 这类调用方不应依赖的长链导航。修复：在第一个对象上提供一个方法，隐藏这段遍历。
- **中间人**：一个类或函数几乎只做转发。修复：去掉它，直接调用真正的目标。
- **被拒绝的遗赠**：子类或实现类忽略或覆盖了继承来的大部分内容。修复：放弃继承，改用组合。

### 4. 并行派出两个子代理

两个子代理的提示都包含下面的**评审守则**：

> 本次评审对当前检出只读：不修改工作区、暂存区、HEAD 或分支状态。需要查看其他版本时，使用 `git show`，或检出到单独的临时目录。评审由你亲自完成，不再派子代理。
>
> 每条发现都写明 `文件:行号`、问题是什么、为什么重要、怎么修复（不明显时）。按实际严重度分级：
>
> - **严重**：bug、安全问题、数据丢失风险、功能损坏。
> - **重要**：不修复就无法信任这项改动，例如遗漏需求、吞掉错误、断言为空的测试、逻辑块原样重复。
> - **次要**：打磨类建议，以及“覆盖可以更广”之类的建议。
>
> 只看 diff 无法核实的要求（位于未改动的代码中，或跨越多处改动），标为 ⚠️ 并说明需要检查什么，不要遍历整个代码库。直接以结论开头，不写铺垫和收尾总结。

**规范子代理**的提示还包含：

- 评审材料文件路径。
- 第 3 步找到的规范来源文件清单，**以及第 3 步代码坏味道基线的全文**（子代理没有其他途径获得它）。
- 任务：“按文件或 hunk 报告：(a) diff 违反成文规范的每一处，并引用规范（文件和条目）；(b) 发现的每个基线坏味道，写出名称并引用 hunk。区分硬性违规和判断性建议：成文规范的违反可以是硬性的，基线坏味道始终是判断性建议，成文规范优先于基线。工具已经强制检查的内容不报告。400 字以内。”

**规格子代理**的提示还包含：

- 评审材料文件路径。
- 规格的路径或获取到的内容，以及事实来源（范围清单、设计依据）的路径。
- 任务：“报告：(a) 规格要求但缺失或只完成一部分的内容；(b) diff 中有、但规格没有要求的行为（范围蔓延）；(c) 看起来已经实现、但实现方式似乎不对的内容；(d) 规格或任务列出的范围项中，缺失的状态，或只打通一条链路却没有覆盖其余变体的情况；(e) 界面改动与设计依据不一致之处，只看 diff 无法对比截图时标为 ⚠️，写明需要对比哪些状态。每条发现都引用规格原文或范围项编号。400 字以内。”

规格缺失时跳过规格子代理，并在最终报告中注明。

### 5. 汇总

把两份报告分别放在 `## 规范` 和 `## 规格` 标题下，原样呈现或稍作整理。**不要**合并两份报告，也不要跨维度重新排序发现，两个维度是有意分开的（见“为什么分两个维度”）。

每个维度末尾给出该维度的结论：**可合并**、**修复后可合并**或**不可合并**，并附一两句技术理由。⚠️ 项由你亲自核实，因为你掌握子代理没有的跨改动上下文；确认是真实缺口的，按规格失败处理。

最后一行总结每个维度的发现数量，以及*每个维度内*最严重的问题（如有）。不要跨维度选出唯一的“最严重”问题，分开报告正是为了避免这种重新排序。

收到评审结果后如何处理，调用 Skill 工具，参数为 `receiving-code-review`。

## 为什么分两个维度

一项改动可能在一个维度通过，却在另一个维度失败：

- 遵守了每一条规范，却实现了错误的功能：**规范通过，规格失败。**
- 完全实现了 issue 的要求，却破坏了项目约定：**规格通过，规范失败。**

分开报告，一个维度的结果就不会掩盖另一个维度的问题。
