---
name: wait-what
description: 停。刚才那段没看懂，请重新讲一遍。
disable-model-invocation: true
---

等等，我没跟上你刚才讲到哪了。重新讲一遍：先补一点背景，用简单直白的短句（每句一个意思，主动语态，不用行话），并使用 `CONTEXT.md` 里的通用语言（仓库有多个上下文时，顺着 `CONTEXT-MAP.md` 找到对应的那个）。
